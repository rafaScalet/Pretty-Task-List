import buttons from './Buttons';
import containers from './Container';
import texts from './Texts';
import inputs from './Inputs';
export const { actionButton, addButton, logInButton } = buttons;
export const { addTaskContainer, cardContainer, container } = containers;
export const { dueDateText, taskTitle, textButton, title } = texts;
export const { input } = inputs;