import { StyleSheet } from 'react-native';

const texts = StyleSheet.create({
  title: {
    fontSize: 50,
    fontWeight: 'bold',
  },
  textButton: {
    color: 'white',
    fontWeight: 'bold',
  },
});

export const { textButton, title } = texts;