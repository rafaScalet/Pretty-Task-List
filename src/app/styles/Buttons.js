import { StyleSheet } from 'react-native';

const buttons = StyleSheet.create({
	logIn: {
		borderRadius: 50,
    backgroundColor: '#a91101',
    alignItems: 'center',
    justifyContent: 'center',
    height: 40,
    width: 100,
    margin: 12,
    padding: 10,
	}
});

export const logInButton = buttons.logIn;