import { StyleSheet } from 'react-native'

const inputs = StyleSheet.create({
	input: {
		height: 40,
		margin: 12,
		borderWidth: 1,
		padding: 10,
		borderRadius: 8,
	},
});

export const input = inputs.input;